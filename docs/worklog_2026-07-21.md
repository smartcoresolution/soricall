# SoriCall Worklog — 2026-07-21

## 관리자 콘솔

- `/admin/login` 전용 관리자 로그인 흐름과 세션 저장을 구현했다.
- SoriMemo를 참조한 데스크톱 관리자 화면을 좌측 계층형 메뉴와 우측 상세 영역 구조로 재설계했다.
- 사용자, 가족 인증정보, 통화 보안, 신고·사건, 개인정보, 통계·보고서, 시스템 관리 메뉴를 구성했다.
- 목록 검색, CSV 다운로드, 상세 드로어, 새로고침, 로그아웃을 연결했다.
- 관리자 overview API를 추가하고 개인정보를 마스킹하며 관리자 조회를 감사 로그에 기록하도록 했다.
- 운영 정적 경로 `/var/www/soricall-subdomain`에 최신 웹 번들을 배포했다.

## 가족 등록정보 관리

- 가족·보호 대상 등록 후 삭제 기능을 보완했다.
- 등록된 가족의 음성 및 얼굴 프로필 삭제 흐름을 추가했다.
- 삭제 상태가 초대 및 등록 현황 화면에 반영되도록 상태 처리를 정리했다.

## 인증 및 배포

- 관리자 ID 기반 로그인 API와 관리자 계정 생성·비밀번호 변경 도구를 추가했다.
- `soricall.ansimsori.ai` 전용 Nginx 설정과 운영 배포 문서를 정리했다.
- 서비스 워커가 최신 번들을 확인하도록 업데이트 캐시 동작을 보완했다.
- 공개 URL에서 관리자 로그인, OpenAPI, overview API 응답을 검증했다.

## 데이터베이스 환경 정리

- 실행 디렉터리에 따라 서로 다른 `soricall.db`가 생성되던 상대경로 SQLite 기본값을 제거했다.
- 개발과 운영에서 `DATABASE_URL`을 필수화하고 PostgreSQL만 허용했다.
- 자동 테스트만 `/tmp`의 격리 SQLite를 사용하도록 분리했다.
- 개발 및 운영 PostgreSQL 볼륨을 별도로 구성했다.
- 명시적 `--confirm-reset`이 필요한 PostgreSQL 초기화 도구를 추가했다.
- 개발 PostgreSQL의 기존 데이터를 초기화하고 `admin` 관리자 계정을 재생성했다.
- PostgreSQL의 `consent_logs.accepted_at`과 ORM 속성 매핑 불일치를 수정했다.

## 현재 실행 상태

- 공개 웹과 API는 새 코드가 반영된 개발 컨테이너에서 정상 응답한다.
- 개발 데이터는 관리자 1개 외에 사용자·가족·통화 데이터가 없는 초기 상태다.
- 운영 PostgreSQL 볼륨은 개발 볼륨과 분리되어 있다.
- 운영 AI는 실제 speaker verification, anti-spoofing, STT 공급자가 연결되지 않아 안전검증이 기동을 차단한다.
- 모의 AI의 운영 허용으로 안전장치를 우회하지 않았으며 실제 모델 연결 전까지 운영 전환을 보류했다.

## 검증

- API 테스트: `64 passed`
- 웹 빌드: TypeScript 및 Vite production build 성공
- 관리자 로그인: 성공
- `/api/v1/admin/overview`: 성공
- 개발 컨테이너: API, AI, PostgreSQL healthy

## 후속 작업

- 실제 운영 AI 모델 공급자와 자격정보를 연결한다.
- 운영 컨테이너를 기동하고 운영 PostgreSQL을 초기화한다.
- 운영 관리자 계정을 재생성한 뒤 공개 URL을 운영 API로 전환한다.
