# Legacy UI archive

Archived on 2026-07-14 before the SoriCall Web and Android screen redesign.

- `web/`: previous React entry screen and stylesheet
- `android/`: previous Compose activities, feature screens, and theme

These files are outside the active Web `src` entry point and Android `app/src/main` source tree,
so they are retained for reference but excluded from application builds.
